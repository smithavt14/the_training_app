<view class="page__container">
    <view 
    class="container-header" 
    style="background-image: url('{{illustrations[workout.category]}}')">
        {{workout.name}}
    </view>

    <view class="container-btn">
        <view class="btn btn-primary">JOIN</view>
    </view>
</view>